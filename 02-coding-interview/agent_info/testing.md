# Testing Strategy

This project uses **Jest** for backend tests and **Vitest** for frontend tests.

## Running Tests

To run all tests:

```bash
npm run test
```

only backend tests:
```bash
npm run test --prefix server
```

only frontend tests:
```bash
npm run test --prefix client -- --run
```
